services:
  - type: web
    name: solana-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: HELIUS_API_KEY
        sync: false
      - key: DATABASE_URL
        sync: false
